# update homebrew
alias brewu='brew update && brew upgrade && brew doctor'

# chrome, clear cache
alias chromeclear='mv ~/Library/Caches/Google/Chrome/Default/* ~/.Trash/'

# cd to default Code directory
alias cdcode='cd ~/Dropbox/Work/personal/code'
